<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="music.dexterous.com.dexterousmusic">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Glide Module and cache -->
    <meta-data
        android:name="music.dexterous.com.dexterousmusic.utils.image.SetupGlideModule"
        android:value="GlideModule" />
    <meta-data
        android:name="music.dexterous.com.dexterousmusic.utils.image.LimitCacheSizeGlideModule"
        android:value="GlideModule" />

    <application
        android:name=".GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.ScanMusicService"
            android:exported="false" />
        <service
            android:name=".service.DexterousPlayMusicService"
            android:exported="false" />


        <!-- It will fire when headset inserted or removed from phone -->
        <receiver
            android:name=".receiver.HeadsetBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.HEADSET_PLUG" />
            </intent-filter>
        </receiver>
    </application>

</manifest>